import { Box, Text } from "@chakra-ui/react";
import { SpeechSynthesizerPanel, getDistinctVoices } from "./SpeechSynthesizer";

// code can go in here, and even an editor that 
// you can write code, then see if it will work

function CodeReference() {



  return (
    <Box padding={5}>
      <Text>Your code reference goes here.</Text>
    </Box>
  );
}

export default CodeReference;
